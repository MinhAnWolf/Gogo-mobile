import { Text, View } from "react-native";
import { ScreenNavigationProp } from "../../type/type-screen";

const DetailSearchLocation: React.FC<ScreenNavigationProp> = ({
  navigation,
}) => {
  return (
    <View>
      <Text>Detail Search Location</Text>
      {/* Thêm logic hoặc các thành phần khác ở đây */}
    </View>
  );
};
export default DetailSearchLocation;
