import { StyleSheet } from "react-native";

export const SearchLocaitonStyles = StyleSheet.create({
  searchContainer: {
    marginLeft: 20,
    margin: 20,
  },

  historySearch: {
    marginTop: 25,
  },
});
